#include "main.h"
#include <string.h>
#include <stdlib.h>

/**
* _atoi - converts string to integer
* @s: string character
* Return: 0;
*/

int _atoi(char *s)
{
int i;
i = atoi(*(s + i));

return (0);
}
