#include "main.h"

/**
* char *_strcpy - copying string variable
* @dest: destination
* @src: source
* Return: string
*/

char *_strcpy(char *dest, *src)
{
int = i;
for (i = 0; src[i] != '\0'; i++)
{
dest[i] = src[i];
}
return (dest);
}