#include <stdio.h>

/**
  * main - prints its ownn name
  * @argv: argument variable
  * @argc: argument count
  * Return: name
  */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
